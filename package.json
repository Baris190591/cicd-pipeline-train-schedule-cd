{
  "name": "cicd-pipeline-train-schedule-git",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "test": "./node_modules/.bin/mocha"
  },
  "dependencies": {
    "cookie-parser": "^1.4.6",    // Updated patch version
    "debug": "^4.3.3",            // Updated to latest major version
    "express": "^4.17.2",         // Updated minor version
    "http-errors": "^1.8.0",      // Updated minor version
    "pug": "^3.0.2",              // Replaced 'jade' with 'pug' (jade's new name)
    "lowdb": "^1.0.0",            // Kept as-is, check if newer versions are suitable
    "morgan": "^1.10.0",          // Updated minor version
    "formidable": "^2.0.1"        // Replaced formidable with updated version
  },
  "devDependencies": {
    "mocha": "^9.1.3",            // Updated major version
    "supertest": "^6.1.6",        // Updated major version
    "jstransformer": "^1.0.0"     // Replaced deprecated 'transformers'
  }
}
